# Local Next.js dev (browser → backend on host)
# Public backend URL (ngrok). Used at build and runtime — never backend:8080 in the browser.
API_URL=https://your-subdomain.ngrok-free.dev
NEXT_PUBLIC_API_URL=https://your-subdomain.ngrok-free.dev
