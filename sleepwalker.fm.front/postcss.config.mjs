/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 uses the @tailwindcss/postcss plugin for utility generation.
 *
 * Add additional PostCSS plugins here if needed.
 * For example:
 *
 * import postcssNested from 'postcss-nested'
 * export default { plugins: [postcssNested()] }
 *
 * Keep this file minimal to avoid double-processing.
 */
export default {
	plugins: {
		'@tailwindcss/postcss': {},
	},
};
